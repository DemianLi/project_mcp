{
  "name": "project-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "An MCP server exposing GitHub platform operations as Tools, speaking MCP 2026-07-28",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "bin": {
    "project-mcp": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck && npm run build && node --import tsx --test \"src/**/*.test.ts\" \"test/**/*.test.ts\"",
    "start": "node dist/main.js",
    "ask": "npm run build --silent && node scripts/ask.mjs",
    "inspect": "npm run build --silent && npx -y @modelcontextprotocol/inspector@2.7.0 node dist/main.js",
    "inspect:cli": "npm run build --silent && npx -y @modelcontextprotocol/inspector@2.7.0 --cli node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
